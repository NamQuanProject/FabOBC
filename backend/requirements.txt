# --- FastAPI gateway ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- Database: Supabase over REST, no direct Postgres connection ---
supabase>=2.10

# --- Agents: Google ADK + Gemini ---
# The [a2a] extra pulls in the a2a-sdk version ADK's to_a2a() actually
# targets (ADK 2.x wants a2a-sdk>=0.3.4,<0.4 - the 1.x line is a breaking
# protobuf-based rewrite that ADK 2.4's agent_to_a2a helper doesn't support
# yet). Keep these two pins in sync when bumping google-adk.
google-adk[a2a]>=2.0,<3.0
google-genai>=1.0

# --- MCP tool servers ---
mcp>=1.2

# --- A2A client/server plumbing ---
a2a-sdk>=0.3.4,<0.4
httpx>=0.27
